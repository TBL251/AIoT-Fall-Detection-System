# ============================================================
# Python 3.10.11  |  Windows 10/11  |  NVIDIA GPU
# ============================================================
#
# REQUIRED system installs BEFORE pip (not handled by pip):
#   1. NVIDIA Driver        >= 527.41
#   2. CUDA Toolkit           12.1     https://developer.nvidia.com/cuda-12-1-0-download-archive
#   3. cuDNN                   8.9     https://developer.nvidia.com/cudnn
#
# Install order for this file:
#   Step 1 — PyTorch GPU (must come FIRST, uses its own index):
#     pip install torch==2.3.1+cu121 torchvision==0.18.1+cu121 torchaudio==2.3.1+cu121  --index-url https://download.pytorch.org/whl/cu121
#
#   Step 2 — Everything else:
#     pip install -r requirements.txt
#
# Verify GPU is visible after install:
#   python -c "import torch; print('PyTorch CUDA:', torch.cuda.is_available())"
#   python -c "import tensorflow as tf; print('TF GPUs:', tf.config.list_physical_devices('GPU'))"
# ============================================================

# ── Core numeric ─────────────────────────────────────────────
numpy==1.26.4

# ── Computer Vision ──────────────────────────────────────────
opencv-python==4.10.0.84
opencv-contrib-python==4.10.0.84
imutils==0.5.4

# ── AI / Deep Learning ───────────────────────────────────────
# TF 2.16 ships Keras 3 internally.
# Do NOT add a standalone keras line — causes duplicate-symbol crashes.
tensorflow==2.16.2

# PyTorch GPU wheels are installed via the Step 1 command above.
# These lines tell pip "already satisfied" so it does not pull CPU-only wheels.
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

ultralytics==8.2.103

# mediapipe 0.10.14 — last release with Windows + TF 2.16 + Python 3.10 support
mediapipe==0.10.14

# protobuf: mediapipe 0.10.14 requires >=4.25.3,<5  — TF 2.16 has no upper cap
# 4.25.3 is the minimum that satisfies both; pin to the exact floor version.
protobuf==4.25.3

# ── Science / ML ─────────────────────────────────────────────
scipy==1.13.1
scikit-learn==1.5.2

# ── Visualisation ────────────────────────────────────────────
matplotlib==3.9.2
pillow==10.4.0

# ── ONNX ─────────────────────────────────────────────────────
# onnxruntime-gpu replaces onnxruntime — both cannot be installed at the same time
onnx==1.16.2
onnxruntime-gpu==1.18.1

# ── Web ──────────────────────────────────────────────────────
flask==3.0.3
flask-socketio==5.3.6
python-socketio==5.11.4
python-engineio==4.9.1
# eventlet 0.36.1 fixes Python 3.10 SSL monkey-patching bug present in 0.35
eventlet==0.36.1

# ── Network ──────────────────────────────────────────────────
requests==2.32.3
# urllib3 2.x breaks pyrebase4 and python-telegram-bot==13.15
urllib3==1.26.20

# ── Telegram ─────────────────────────────────────────────────
python-telegram-bot==13.15

# ── Serial ───────────────────────────────────────────────────
pyserial==3.5

# ── Firebase ─────────────────────────────────────────────────
firebase-admin==6.5.0
google-cloud-storage==2.18.2
pyrebase4==4.8.0

# ── Environment ──────────────────────────────────────────────
python-dotenv==1.0.1

# ── Security ─────────────────────────────────────────────────
Werkzeug==3.0.4
cryptography==43.0.3

# ── Video ────────────────────────────────────────────────────
ffmpeg-python==0.2.0
imageio==2.35.1
imageio-ffmpeg==0.5.1

# ── Async ────────────────────────────────────────────────────
aiohttp==3.10.5
aiofiles==24.1.0

# ── Utilities ────────────────────────────────────────────────
tqdm==4.66.5
colorama==0.4.6
tinydb==4.8.0